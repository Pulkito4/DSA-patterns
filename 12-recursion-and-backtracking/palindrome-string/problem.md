# Palindrome String

- Source: [GeeksforGeeks](https://www.geeksforgeeks.org/problems/palindrome-string0817/1)
- Platform: GeeksforGeeks

## Problem Description

Given a string `s`, find if it is a palindrome. A string is considered a palindrome if it reads the same forwards and backwards.

## Examples

### Example 1:
- **Input:** `s = "abba"`
- **Output:** `true`
- **Explanation:** `"abba"` reads the same forwards and backwards, so it is a palindrome.

### Example 2:
- **Input:** `s = "abc"`
- **Output:** `false`
- **Explanation:** `"abc"` does not read the same forwards and backwards, so it is not a palindrome.

## Constraints

- `1 <= s.size() <= 10^6`
- `s` contains: `[a-z]`
